import { VillageTypePipe } from './village-type.pipe';

describe('VillageTypePipe', () => {
  it('create an instance', () => {
    const pipe = new VillageTypePipe();
    expect(pipe).toBeTruthy();
  });
});

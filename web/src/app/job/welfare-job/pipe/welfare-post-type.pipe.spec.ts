import { WelfarePostTypePipe } from './welfare-post-type.pipe';

describe('WelfarePostTypePipe', () => {
  it('create an instance', () => {
    const pipe = new WelfarePostTypePipe();
    expect(pipe).toBeTruthy();
  });
});

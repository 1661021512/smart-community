import { BirthdayAgePipe } from './birthday-age.pipe';

describe('BirthdayAgePipe', () => {
  it('create an instance', () => {
    const pipe = new BirthdayAgePipe();
    expect(pipe).toBeTruthy();
  });
});
